# MNIST CNN (using PyTorch)

A simple convolutional neural network implemented in PyTorch for handwritten digit classification on the MNIST dataset.  
Typically achieves ~99% test accuracy.

## How to run
```bash
pip install -r requirements.txt
python mnistcnn.py
